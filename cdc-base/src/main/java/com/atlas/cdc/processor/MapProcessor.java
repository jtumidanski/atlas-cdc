package com.atlas.cdc.processor;

public final class MapProcessor {
   private MapProcessor() {
   }

   public static void spawnItem(int mapId, int characterId, int itemId) {
      //      map.spawnItemDrop(chr, chr, new Item(loseItem.itemId(), (short) 0, (short) 1),
      //            map.calcDropPos(pos, chr.position()), true, true);
   }

   public static void removeMonster(int mapId, int monsterUniqueId) {

   }
}
